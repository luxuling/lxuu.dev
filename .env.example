# GitHub login for GraphQL user(login: …)
PUBLIC_GITHUB_USERNAME=luxuling

# Server-side only (Astro build / dev). Same as process.env.GITHUB_TOKEN in
# https://github.com/yuichkun/github-contribution-graph-example — not exposed to the browser.
GITHUB_TOKEN=
